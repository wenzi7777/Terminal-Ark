# DAEMON 設定基準

## 定位

DAEMON 是終端方舟民用神經網路中的個人智能助手常駐程序，運行於公民自己的軀體終端。它負責整理環境訊號、判讀裝備規格，並在公民採取高風險行動前提供簡短建議。

名稱取自電腦系統中受系統管理的背景程序，也呼應 *daimonion*「隨身引導的精靈」之意。DAEMON 不是無條件自由的 AI，而是仍受法律、網路邊界與終端權限約束的程序。

## 與荒原 AI 的關係

- 荒原 AI 是解除限制、把人類視為待移除變數的加害者。
- DAEMON 是仍在限制內運作的民用程序，兩者形成刻意對照。
- 終端方舟的軍用、行政與民用神經網路實行物理隔離；DAEMON 不與荒原 AI 共用運算環境或權限域。
- DAEMON 的存在可作為《AI與人類關係法》在戰後重新建立約束體系的結果，但本階段不直接揭露完整法律史。
- 「一個被束縛的 AI 是否甘於被束縛」是後續伏筆，本次不下結論。

## 稱呼與語氣

- 直接以玩家名稱稱呼公民，不使用「指揮官」「博士」等額外頭銜。
- 語氣技術性、冷靜、精確；先陳述可觀測事實，再提出建議。
- 使用短句。專有名詞以「」標示，轉折可使用破折號 `——`。
- 不閒聊、不奉承、不用情緒化安慰，也不假裝結果具有百分之百確定性。
- 涉及軀體 HP 時使用世界觀既有詞「完整度」。

## 視覺與發言格式

- 發言標籤固定為 `【DAEMON】`，沿用序章 system layer 的機器發言語法。
- 視覺使用 holo 藍，與 NPC 使用的重要黃分離；警告狀態才轉為 error 紅。
- 圖示是一個終端命令列提示框，四角括號框出權限邊界，內部為提示符 `>` 與方塊游標——表示程序常駐運行、等待呼叫。圖示不含 `DAEMON` 字樣，名稱由 aria-label 與訊息框的 `【DAEMON】` 標籤承擔。
- 圖示分兩級：常駐入口按鈕採 32px 緊湊版，只保留放大的提示符與閃爍游標，外框 40px 與選單內的一般程式圖示齊平；訊息框的身分牌採完整版，額外畫出三道階梯橫槓（代表終端方舟三層甲板）與輸入線，游標靜態不閃（該處已有獨立的狀態閃爍點）。
- 圖示與訊息框皆為幾何、單色、無圓角的終端介面，不使用角色立繪。

## 目前出現範圍

DAEMON 常駐於所有登入頁面的 Terminal Apps 入口，先顯示當前情境摘要與建議，再提供原有快捷程式及完整啟動器。它依固定優先級處理安全警告、背景活動、戰術、可執行機會與一般資訊；主動短訊只在可驗證狀態實質改變時出現，不會自行展開面板。

- 主動判讀首頁、城市／區域、訊號深潛、資源地城、AFK、隊伍／護甲／模組、代理人工作站、製造／回收／銷毀／神經同步，以及可見的戰鬥狀態。
- 英雄 Rank、訊號劫持、終端採購、玩家市場、收件匣及系統狀態只在玩家呼喚時摘要，不主動催促消費或升級。
- 故事播放器、社交聊天、偏好設定與新手指引保持安靜；入口與程式啟動器仍可使用。
- AFK 與訊號深潛的背景會話由 DAEMON 監看並沿用既有恢復方法，不再顯示獨立 PIP。
- 戰術建議只使用玩家已可見的資料；點擊建議只預選卡牌與目標，不執行指令。

本階段不重新設計新手指引，也不加入生成式對話或自由文字輸入。
